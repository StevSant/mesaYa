# =============================================================================
# MesaYA - n8n Environment Variables (TEMPLATE)
# =============================================================================
# Copia este archivo a .env y configura los valores reales

# ---------------------------------------------------------------------------
# Configuración de n8n
# ---------------------------------------------------------------------------
N8N_USER=admin
N8N_PASSWORD=mesaya_n8n_2024
N8N_WEBHOOK_URL=http://localhost:5678/
# Generar con: openssl rand -hex 32
N8N_ENCRYPTION_KEY=cambiar_esta_clave_en_produccion_32chars
N8N_LOG_LEVEL=info
TZ=America/Guayaquil

# ---------------------------------------------------------------------------
# URLs de Microservicios MesaYA
# ---------------------------------------------------------------------------
# Backend principal (NestJS) - Puerto 3000
MESAYA_API_URL=http://host.docker.internal:3000

# WebSocket service (Go) - Puerto 8080
MESAYA_WS_URL=http://host.docker.internal:8080

# Chatbot service (FastAPI) - Puerto 8001
MESAYA_CHATBOT_URL=http://host.docker.internal:8001

# GraphQL service (Strawberry) - Puerto 8000
MESAYA_GRAPHQL_URL=http://host.docker.internal:8000/graphql

# Payment service (FastAPI) - Puerto 8003
MESAYA_PAYMENT_URL=http://host.docker.internal:8003

# ---------------------------------------------------------------------------
# Webhooks Externos
# ---------------------------------------------------------------------------
PARTNER_WEBHOOK_URL=https://partner.example.com/webhook
# Generar con: openssl rand -hex 32
PARTNER_WEBHOOK_SECRET=change-this-secret-to-match-partner-shared-key

# ---------------------------------------------------------------------------
# Kafka
# ---------------------------------------------------------------------------
MESAYA_KAFKA_BROKERS=kafka:9092

# ---------------------------------------------------------------------------
# Credenciales SMTP (Gmail ejemplo)
# ---------------------------------------------------------------------------
# Para Gmail: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=tu-app-password-aqui
SMTP_FROM=pagos@mesaya.com

# ---------------------------------------------------------------------------
# Credenciales Telegram Bot
# ---------------------------------------------------------------------------
# Obtener de @BotFather en Telegram
TELEGRAM_BOT_TOKEN=tu-bot-token-aqui

# ---------------------------------------------------------------------------
# Credenciales Email IMAP
# ---------------------------------------------------------------------------
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=soporte@mesaya.com
IMAP_PASSWORD=tu-app-password-aqui
